language: python
python:
    - "3.6"

services:
    - docker

before_script: pip install docker-compose

branches:
    only:
        - master

deploy:
    provider: script
    access_key_id: ${AWS_ACCESS_KEY_ID}
    secret_access_key: ${AWS_SECRET_ACCESS_KEY}
    script: bash pull_script.sh